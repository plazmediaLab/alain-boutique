import React from 'react';

export default function GroupList(){
  return (
    <ul>
      {/* { groups.map((item, index) => (
        <li
          key={ index }
        >{ item }</li>
        ))
      } */}
    </ul>
  );
};